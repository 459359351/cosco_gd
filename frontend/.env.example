# 复制为 .env 后按说明填写。Vite 仅暴露以 VITE_ 开头的变量。

# 高德开放平台「Web端(JS API)」Key：https://console.amap.com/dev/key/app
# 需在控制台绑定域名/安全策略；地图组件加载失败时首先检查此项
VITE_AMAP_KEY=

# 若控制台启用了「安全密钥」(securityJsCode)，在此填写；当前代码加载器未传该字段时也可先留空试跑
# 若控制台要求必须校验，需在高德文档中配置，并在 useAMapLoader 里传入 securityJsCode
VITE_AMAP_SECURITY=

# Axios 请求前缀。开发时用 /api/v1，由 vite.config 代理到本机 8000
# 生产部署若前后端同域可继续 /api/v1；若跨域则写完整地址，如 https://api.example.com/api/v1
VITE_API_BASE=/api/v1

# WebSocket 地址。开发时建议留空：会使用「当前页面」的 host（如 ws://localhost:5173）+ /ws/realtime，走 Vite 代理到后端
# 若必填场景：开发可写 ws://127.0.0.1:5173/ws/realtime；生产同源可写 wss://你的域名/ws/realtime
VITE_WS_BASE=
