把多周周报加到历史数据里，加一个日历选项，他们的周报数据是从上周四-本周三，日历选的天落到哪个周期就显示哪个周的周报数据，默认进页面显示最新一周的



翻译:

用户页面可以点击鼠标 切换天数 看起来是天数 实际上是计算属于哪一周  同时要持久化存储多周的excel 上传的报表改为报表list 
日期判断条件为 上周的周四到本周的周三为本周数据
用户进入页面默认显示最后一周

鼠标滑动到某一周 显示这周的block块

admin页面要支持 excel管理

用户页要在合适地方融入切换组件 