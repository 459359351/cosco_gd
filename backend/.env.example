# 应用（FastAPI / SQLAlchemy AsyncSession）使用 asyncpg
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cosco_gd
# Alembic 迁移在 alembic/env.py 内自动把上面地址转为 postgresql+psycopg://（同步），需安装 psycopg
REDIS_URL=redis://localhost:6379/0
CORS_ORIGINS=["*"]
